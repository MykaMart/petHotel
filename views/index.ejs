<!DOCTYPE html>
<html>
<head>
	<meta charset="utf-8">
	<meta http-equiv="X-UA-Compatible" content="IE=8, IE=9, IE=edge"/>
	<link rel="stylesheet" type="text/css" href="/styles/style.css">
	<title></title>
</head>
<body>

	<h1> Red Woof Inn </h1>

	<h3> Current Guests </h3>

		<% guestList.forEach( (guest) => { %>
			<h4> <a href="/guests/<%= guest.id %>/reservation"><%= guest.name %> </a> </h4>

			<form action="/guests/<%= guest.id %>?_method=DELETE" method="POST">
				<button type="submit" name="checkout">Check Out</button>
			</form>

			<form action="/guests/<%= guest.id %>/change">
				<button type="submit" name="change">Change Reservation</button>
			</form>
	<% }) %>

	<form action="/guests/make_reservation">
				<button type="submit" name="makeRes">Make Reservation</button>
			</form>


	<script src="scripts/app.js"></script>
</body>
</html>